language: java
sudo: false
install: true
jdk:
  - oraclejdk8
script:
    - ./gradlew clean build jacocoTestReport
after_success:
  - bash <(curl -s https://codecov.io/bash)
deploy:
  skip_cleanup: true
  provider: azure_web_apps
